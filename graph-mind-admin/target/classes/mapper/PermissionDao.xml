<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenpp.graph.admin.mapper.PermissionDao">

    <select id="selectByParentId" resultType="com.chenpp.graph.admin.model.Permission">
        SELECT 
            permission_id,
            permission_name,
            parent_id,
            permission_type,
            permission_key,
            component,
            path,
            icon,
            order_num,
            status,
            visible,
            cache,
            create_time,
            update_time
        FROM sys_permission
        WHERE parent_id = #{parentId}
        ORDER BY order_num, permission_id
    </select>
</mapper>