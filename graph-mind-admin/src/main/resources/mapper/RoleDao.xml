<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenpp.graph.admin.mapper.RoleDao">

    <select id="selectRolePage" resultType="com.chenpp.graph.admin.model.Role">
        SELECT 
            role_id,
            role_name,
            role_key,
            role_sort,
            status,
            data_scope,
            remark,
            create_time,
            update_time
        FROM sys_role
        <where>
            <if test="roleName != null and roleName != ''">
                AND role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
            <if test="roleKey != null and roleKey != ''">
                AND role_key LIKE CONCAT('%', #{roleKey}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY role_sort, role_id
    </select>
</mapper>